services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: razorflow
      POSTGRES_PASSWORD: razorflow
      POSTGRES_DB: razorflow
    ports:
      - "5432:5432"
    volumes:
      - razorflow_pgdata:/var/lib/postgresql/data

volumes:
  razorflow_pgdata:
